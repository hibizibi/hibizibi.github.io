baseURL = 'http://hibizibi.github.io/'
languageCode = 'en-us'
title = 'Hibizibi'

# Change the default theme to be use when building the site with Hugo
theme = "learn"

# For search functionality
[outputs]
home = [ "HTML", "RSS", "JSON"]